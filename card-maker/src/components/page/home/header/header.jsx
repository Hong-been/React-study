import React from "react";
import styles from "../login_popup/login_popup.module.css";

const Header = (props) => (
	<header className={styles.header}>Wellcome to Bisiness Card Maker ✨</header>
);

export default Header;
