import React from "react";
import styles from "../login_popup/login_popup.module.css";

const Footer = (props) => (
	<footer className={styles.footer}>Create your card! </footer>
);

export default Footer;
